[Todo]

1. Casting 연산자
	- 데이터 타입을 다른 타입으로 일시적으로 변경하는 것.
	1) 암시적 형변환 : 자바 가상 머신이 자동으로 데이터 형을 변경함.
		ex) double d = 100 ;
		* 정수가 실수로 자동 형변환(정수로 보이지만 100.0인 상태).
		* 정수 -> 실수(작은 그릇에서 큰 그릇으로) 변형 가능.
	2) 명시적 형변환 : 개발자가 직접 캐스팅함.
		ex) int i = (int)12.5
		* 12.5를 int형으로 바꿔서 변수 i에 저장한다는 뜻.
		* 정수 부분의 값만 취하고 0.5 부분은 버림, 변수 i 는 정수 12가 됨.
	
	- 문자간의 비교는 자바 가상 버신이 정수형으로 형변환 후 수행하여 비교.
		* 아스키 코드 : 알파벳(문자)을 10진수로 만들어서 컴퓨터 내부에서 처리하기 위한 코드.
						-> 내부적으로(암시적으로) 정수형으로 캐스팅된다는 뜻.
						* 숫자 0 = 48 , A = 65, a = 97 정도는 암기
		(1) 형변환이 되므로 문자간의 뺄셈 가능.
			int x = 'a' - 'A' = 97 - 65 = 32 ;
		(2) 문자 간의 비교, 판단 가능.
	
	<예시>
	(1)	(double)14/5 ---> 14.0/5 ---> 14.0 / 5.0 ---> 2.8
					명시적       암시적

    (2)	(double)(14/5) ---> (double)(2) ---> 2.0
				     산술연산            명시적
    
	(3)	char ch1 = 'c'; // 정수로 99
		char ch2 = 'a'; // 정수로 97
		boolean bool1 = ch1 > ch2 ; // 암시적 형변환으로 99 > 97 -> true
    
	(4)	char ch3 = 'D' ;
		String msg;
		msg = 'D' >= 'A' && ch3 <='Z' ? "Yes" : "No" ;
    